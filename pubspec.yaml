name: io_fish
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc:
  equatable:
  flutter_shimmer:
  font_awesome_flutter:
  mqtt_client:
  amplify_flutter:
  amplify_auth_cognito:
  amplify_authenticator:
  amplify_core:
  amplify_datastore:
  amplify_api:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/utils/certs/root_ca.pem
    - lib/utils/certs/device_certificate.crt
    - lib/utils/certs/private.key
